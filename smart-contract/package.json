{
  "name": "property-document-verifier-contract",
  "version": "1.0.0",
  "private": true,
  "description": "Solidity smart contract for document hash store and verification",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.18"
  }
}
